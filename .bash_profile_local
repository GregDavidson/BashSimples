# ~/.bash_profile_local - system and user variable Bash Profile
# Sourced by ~/.bash_profile

if_src_dir ~/.bash_profile_local.d

# This is a good place to add favorites to your PATH variables, e.g.
# shopt -s nullglob
# path_add -aDV ~/SW/*/[Bb]in{,`arch`} /usr/bin/mh ~/.cargo/bin /usr/local/SW/*/[Bb]in
# shopt -u nullglob